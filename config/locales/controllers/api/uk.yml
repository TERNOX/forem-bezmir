# Original locale: controllers/api/en.yml
---
uk:
  api:
    v0:
      analytics_controller:
        invalid_date_format: Date parameters 'start' або 'end' must be у the format з 'yyyy-mm-dd'
        start_missing: Required 'start' parameter є missing
      articles_controller:
        must_be_json: article param must be a JSON object. Ви provided article as a %{type}
      follows_controller:
        followed:
          one: followed %{count} користувач
          other: followed %{count} користувачі
      health_checks_controller:
        app_is_up: App є up!
        database_connected: Database connected
        database_not_connected: Database NOT connected!
        redis_connected: Redis connected
        redis_not_connected: Redis NOT connected!
      listings_controller:
        no_credit: Not enough available credits
