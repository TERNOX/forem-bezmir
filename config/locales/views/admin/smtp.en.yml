---
en:
  views:
    admin:
      smtp:
        automatic_digests_enabled: Send automatic digests
        automatic_digests_description: Turn off to stop automatic email digests. Sign-in, password reset, and other messages continue to send. SMTP settings are kept, and you can still send a test digest manually.
