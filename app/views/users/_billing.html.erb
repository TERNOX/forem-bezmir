<% provider = Settings::General.payment_provider.to_s.presence || "stripe" %>
<% partial_name = provider == "monobank" ? "monobank" : "stripe" %>
<%= render "users/billing/#{partial_name}", customer: @customer %>
